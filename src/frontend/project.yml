name: MindFlow
options:
  bundleIdPrefix: com.mindflow
  deploymentTarget:
    macOS: "12.0"
  xcodeVersion: "15.0"
  generateEmptyDirectories: true

settings:
  base:
    PRODUCT_NAME: MindFlow
    MARKETING_VERSION: "0.2.0"
    CURRENT_PROJECT_VERSION: "1"
    SWIFT_VERSION: "5.9"
    MACOSX_DEPLOYMENT_TARGET: "12.0"
    CODE_SIGN_IDENTITY: "-"
    CODE_SIGN_STYLE: Manual
    ENABLE_HARDENED_RUNTIME: YES

targets:
  MindFlow:
    type: application
    platform: macOS
    sources:
      - path: main.swift
      - path: AppDelegate.swift
      - path: HotkeyManager.swift
      - path: InputPanel.swift
      - path: CandidateWindow.swift
      - path: StatusMenuController.swift
      - path: APIClient.swift
    settings:
      base:
        INFOPLIST_FILE: MindFlow/Info.plist
        PRODUCT_BUNDLE_IDENTIFIER: com.mindflow.app
        LD_RUNPATH_SEARCH_PATHS: "$(inherited) @executable_path/../Frameworks"
        COMBINE_HIDPI_IMAGES: YES
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
    entitlements:
      path: MindFlow/MindFlow.entitlements
      properties:
        com.apple.security.app-sandbox: false
